# Content intelligence patterns

Inspired by Go Viral Bro style systems:

```text
Discover -> Angle -> Script -> Post/Draft -> Analyze
```

Operating rules:

- Use competitor winners only as pattern inspiration, never direct copying.
- Capture reusable structures, not exact wording.
- For each winning pattern, extract:
  - hook skeleton;
  - audience promise;
  - proof angle;
  - CTA;
  - platform fit;
  - why it likely worked;
  - how to adapt it for GridFactory.io or Gulf-EL/NexRide.
- Prefer concise reusable pattern notes over long transcripts.
